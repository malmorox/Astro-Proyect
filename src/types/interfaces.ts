export interface NavItem {
    title: string;
    url: string;
}

export interface Product {
    name: string;
    description: string;
    image: string;
    price: number;
    type: string;
}

export interface FaQ {
    question: string;
    response: string;
}